{
  "meta": {
    "name": "mac-dev-common",
    "version": "1.0.0",
    "description": "Shared macOS local development access: repos, runtimes, caches; personal paths denied"
  },
  "extends": "default",

  "groups": {
    "include": [
      "node_runtime",
      "python_runtime",
      "unlink_protection"
    ],
    "exclude": [
      "dangerous_commands",
      "dangerous_commands_macos"
    ]
  },

  "filesystem": {
    "read": [
      "/usr/local/share/dotnet",
      "/etc/dotnet",
      "$HOME/.config/mise",
      "$HOME/.gitignore"
    ],

    "allow": [
      "$HOME/my-repos",
      "$HOME/other-repos",
      "$HOME/.dotfiles",
      "$HOME/.dotfiles-personal",
      "$HOME/.config/nono",

      "$HOME/.dotnet",
      "$HOME/.nuget/packages",
      "$HOME/.local/share/NuGet",
      "$HOME/Library/Caches/NuGet",

      "$HOME/.local/share/mise",
      "$HOME/.local/state/mise",
      "$HOME/.cache/mise",

      "$HOME/.cache/uv",
      "$HOME/.local/share/uv",
      "$HOME/.npm"
    ],

    "deny": [
      "$HOME/Documents",
      "$HOME/Downloads",
      "$HOME/Pictures",
      "$HOME/Desktop",
      "$HOME/Applications",
      "$HOME/Music",
      "$HOME/.notes"
    ]
  },

  "network": {
    "block": false
  }
}
